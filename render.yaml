services:
  - type: web
    name: waterborne-risk-api
    env: docker
    plan: free
    autoDeploy: true
    dockerfilePath: water_quality/Dockerfile
    envVars:
      - key: MODEL_PATH
        value: water_quality/models/model.pkl
